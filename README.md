## Sanitized minimal version of my dotfiles

**Core Tools** vim, git, zsh, tmux, shellenv

Install via you favorite package manager

Use `stow` to symlink these dotfiles to your `~`

#### Buena suerte, Amigos
